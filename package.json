{
  "name": "raylab-cluster-manager",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "tauri": "tauri",
    "frontend:dev": "corepack pnpm --filter @raylab/frontend dev",
    "build": "tauri build",
    "frontend:build": "corepack pnpm --filter @raylab/frontend build",
    "test": "corepack pnpm --filter @raylab/frontend test && cargo test --manifest-path src-tauri/Cargo.toml",
    "check": "corepack pnpm --filter @raylab/frontend build && cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  },
  "packageManager": "pnpm@9.12.1",
  "pnpm": {
    "overrides": {
      "nanoid@<3.3.18": "3.3.18"
    }
  },
  "workspaces": [
    "frontend"
  ]
}
